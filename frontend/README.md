# UniMatch — frontend, подключён к бэкенду

`app.js` больше не хранит профиль/свайпы в `localStorage` как основную базу —
теперь это делает бэкенд (FastAPI). `localStorage` хранит только лёгкий
"указатель" `{userId, guestId, lang}`, чтобы не заставлять человека проходить
онбординг заново при каждом визите.

## Что изменилось

- **`API_BASE`** (в начале файла) — адрес бэкенда, по умолчанию
  `http://127.0.0.1:8000`. Поменяй, если бэкенд крутится на другом порту/хосте.
- **`apiRequest()`** — единая обёртка над `fetch`: если бэкенд недоступен
  (не запущен / сеть отвалилась), показывает toast с понятным сообщением
  вместо тихого зависания.
- **Онбординг** (`finishOnboarding`) — теперь `POST /users`, получает от
  сервера `id` пользователя и настоящую подборку вузов (`GET /universities`)
  с посчитанными на бэке Match Score / Admission Reality.
- **Свайпы** (`doSwipe`, `toggleLike`) — пишутся на сервер (`POST`/`DELETE
  /users/{id}/swipes`), переживают перезагрузку страницы и работают с любого
  устройства, если знать `userId`.
- **Фильтры на Discover** (континенты, бюджет) — сохраняются на сервер и
  подборка перезапрашивается оттуда же.
- **"Мягкая" регистрация по email** — теперь настоящий `POST /users/{id}/register`.
- **При открытии страницы** — если в localStorage есть `userId`, профиль,
  лайки и подборка подтягиваются с сервера; если бэкенд недоступен —
  аккуратный откат на онбординг, а не белый экран.

## Что осталось локальным (сознательно)

- **`buildWhyReasons` / `matchBreakdown`** ("Why this university", разбивка
  Match Score по категориям) — чистые функции, которые не меняются между
  запросами и не нуждаются в сети. Бэкенд их дублирует (`/universities/{id}/why`),
  но фронт им пока не пользуется — можно переключить позже, если понадобится.
- **`computeDNA()`** — считается локально из уже загруженного `state.liked`
  (который, в свою очередь, приходит с бэкенда). Отдельного запроса не нужно.
- **Счётчик "passed" (дизлайков)** на экране DNA — считается только за
  текущую сессию в браузере, не переживает перезагрузку. У бэкенда пока нет
  эндпоинта для истории дизлайков.
- **Локальный массив `UNIVERSITIES`** остался в файле — служит источником
  статичных данных, которые бэкенд не хранит (сейчас это только `photo` у
  MIT). Все 50 записей там 1:1 совпадают с тем, что вернёт `seed_data.py`.

## Application Tracker + Roadmap

Два новых экрана (один пункт в сайдбаре, два вида на нём — `Tracker` /
`Roadmap`): статус заявки на каждый вуз из Matches и таймлайн подготовки
до дедлайна. Как и `computeDNA()`/счётчик "passed" выше — **полностью
локальная фича**, бэкенд её не касается.

- **Хранилище** — `localStorage`, ключ `unimatch_tracker_v1`, по одной
  записи `{status, checklist}` на id университета. Та же схема, что у
  `THEME_KEY`/`GUEST_KEY`/`ACTIVITY_KEY` в начале `app.js`: `try/catch` на
  случай приватного режима браузера, ничего не падает, если localStorage
  недоступен — просто не сохраняется.
- **Синхронизация с лайками — двусторонняя, через сам `toggleLike()`**, а не
  отдельной логикой: лайкнул вуз на Discover → у него автоматически
  появляется запись трекера (`ensureTrackerEntry`), убрал лайк → запись
  удаляется (`removeTrackerEntry`). Если лайки были сделаны ещё до появления
  этой фичи (или пришли с другого устройства через бэкенд) — при загрузке
  страницы `syncTrackerWithLiked()` сама дозаполняет недостающие записи и
  подчищает те, для которых лайка уже нет.
- **Дедлайн (ED/EA/RD + дата) — правдоподобный плейсхолдер, не факт.** Ни
  бэкенд, ни `UNIVERSITIES` не хранят настоящие даты приёмных кампаний —
  `estimateDeadline()` в `app.js` выводит тип и число эвристически из
  `acceptance` (селективность) и `continent`, ровно так же, как `gpa25`/
  `gpa75`/`sat25`/`sat75` в `admissionReality()` (см. комментарий там же).
  В проде эти даты должны приходить с сайта приёмной комиссии конкретного
  вуза, а не считаться на фронте.
- **Чек-лист документов** (эссе / рекомендации / транскрипт / тесты) —
  4 булевых поля внутри той же записи трекера. **Roadmap использует три из
  них (тесты/рекомендации/эссе) как первые три вехи таймлайна** — это одно
  и то же состояние, single source of truth: отметил чекбокс на Tracker —
  веха на Roadmap тоже закроется, и наоборот. Четвёртая веха, "подача", —
  не чекбокс, а сам статус заявки (клик по ней сразу ставит `submitted`).
- **Статусы заявки** (`not_started → in_progress → submitted → decision`)
  переключаются вручную кликом по пилюле на карточке — не автоматическая
  логика, фронт не пытается угадывать, когда заявка реально подана.
- Полный сброс профиля (`restartOnboarding`) чистит `unimatch_tracker_v1`
  вместе с `GUEST_KEY` — иначе после сброса остались бы сиротские записи
  трекера без соответствующих лайков.

## Важно: бэкенд теперь источник истины

В отличие от исходного демо, приложение **не завершит онбординг**, если
бэкенд не запущен — вместо этого покажет toast с ошибкой. Это осознанный
компромисс этой версии интеграции: раз Match Score/Admission Reality/Matches
считает сервер, без него приложению просто нечего показывать.

## Иконка (favicon)

Раньше фавикон был отдельной картинкой "U" на фиолетовом фоне — цвет никак
не был связан с брендовой палитрой сайта (`--brand-primary` в `styles.css`)
и не менялся вместе с тёмной темой.

Теперь:

- **Форма** — та же, что и у логотипа в шапке приложения (`ICONS.logo` в
  `app.js`), а не отдельная буква.
- **Цвет — не захардкожен.** `buildFaviconSVG(bg, fg)` в `app.js` собирает
  SVG иконки из двух параметров цвета, а `updateFavicon()` берёт их прямо
  из применённых CSS-переменных `--brand-primary` (фон) и
  `--brand-on-primary` (сам значок), так что фавикон:
  - при обычной загрузке совпадает с брендовым зелёным (`#5E7F3C`);
  - автоматически перекрашивается при переключении на тёмную тему
    (`--brand-primary` там уже другой — приглушённый лаймовый `#C6D17C`), и обратно;
  - заодно обновляет `<meta name="theme-color">`, чтобы цвет системного
    тулбара на мобильных тоже совпадал с темой.
- **`index.html`** хранит только статичный фолбэк той же формы/цвета —
  на случай, если картинка запрашивается браузером до того, как выполнится
  `app.js` (у него `id="favicon"`, за него и цепляется `updateFavicon()`).

Поменять цвет иконки насовсем (например, если сменится бренд) — значит
поменять `--brand-primary`/`--brand-on-primary` в `styles.css`: фавикон,
логотип и остальной интерфейс перекрасятся одновременно, без правки самого
SVG.

## Как проверить, что всё работает

1. Запусти бэкенд (см. README бэкенда) — обязательно **до** открытия фронта.
2. Открой `index.html` (Live Server или любой локальный сервер).
3. Пройди онбординг — в Network (фильтр Fetch/XHR) должен появиться
   `POST http://127.0.0.1:8000/users`.
4. Свайпни карточку — появится `POST .../swipes`.
5. Перезагрузи страницу — должна открыться сразу Discover-страница с той же
   подборкой и тем же списком Matches, а не онбординг заново.

Всё это протестировано headless-браузерным прогоном (jsdom) против реального
запущенного бэкенда — полный цикл онбординг → свайп → перезагрузка страницы
→ восстановление профиля/лайков/колоды с сервера, плюс отдельно отмена лайка
и смена фильтра по континенту — так что интеграция не "выглядит рабочей", а
реально проверена сквозным сценарием.

## Widescreen hero + логин/регистрация + фичи вместо 4 шагов

Три отдельных изменения на лендинге, поверх всего описанного выше.

- **Hero — широкая 2-колоночная вёрстка.** `.lp-hero-grid` был стопкой
  (заголовок на всю ширину, телефон вторым рядом ниже); теперь это
  `grid-template-columns: minmax(0,1.2fr) minmax(300px,460px)` — текст и
  телефон делят одну строку, заголовок увеличен и заужен по `max-width`
  (13ch вместо 22ch), чтобы строки реально доходили до края колонки, плюс
  мягкое двухцветное радиальное свечение (`.lp-hero::before`) на всю ширину
  экрана позади. Ниже 1080px сетка схлопывается обратно в одну колонку
  (телефон вторым рядом, центрированным) — старое поведение для планшетов
  и мобильных не тронуто.

- **Экран логина/регистрации (`#login` в index.html)** — полноэкранный
  оверлей в фирменной светлой палитре сайта (`--brand-*`), не тёмной, как
  на референсе. Переключение вход/регистрация — `setAuthMode()`, глазок у
  пароля переиспользует иконки `lockClosed`/`unlock` из `ICONS` (без новых
  SVG). Открывается по ghost-кнопке "Войти" в навбаре
  (`showLoginScreen('login')`), закрывается крестиком или сама скрывается
  при переходе в приложение после успешного входа.

  **Важно про бэкенд.** Текущая интеграция (см. выше) — гостевая модель:
  `userId`/`guestId`, "регистрация" — это мягкий `POST /users/{id}/register
  {email}` **без пароля**. Выделенных ручек с паролем на бэке, судя по
  всему, ещё нет. `apiAuthLogin`/`apiAuthRegister` в `app.js` бьют в
  `POST /auth/login` и `POST /auth/register` с телом `{email, password}` и
  ожидают в ответ объект пользователя (`{id, guestId, ...}`, тот же
  контракт, что у `/users`) — это **предположение о форме ответа**, не
  подтверждённый факт. Если на бэке эти ручки называются иначе или отдают
  другую форму — поправить нужно только `apiAuthLogin`/`apiAuthRegister`
  и, возможно, обработку ответа в `submitAuthForm()`, остальной экран
  (вёрстка, переключение режимов, ошибки) их не касается.

  Ошибка входа/регистрации показывается прямо в форме (`.auth-error`), а
  не общим toast'ом — `authRequest()` (отдельная от `apiRequest()` обёртка)
  пробрасывает текст из `{detail}`/`{message}` тела ответа, если бэкенд
  его вернул (стандартно для FastAPI на 4xx).

  `init()` был отрефакторен: код загрузки профиля/лайков/колоды с сервера
  вынесен в `loadSessionForUser(userId)` — теперь им пользуется и обычная
  загрузка страницы, и `submitAuthForm()` сразу после успешного входа.

- **Секция "4 фичи" вместо "4 шага онбординга" (`#lp-how`).** Технически —
  тот же приём sticky-scroll "наездом", что в присланном макете
  (`.lp-feature-stage-outer` высотой 340vh, `.lp-feature-stage` с
  `position:sticky`, прогресс считается от `getBoundingClientRect()` внешней
  обёртки на `scroll` у `window`). Но содержимое карточек — не копия CSS из
  макета, а настоящие компоненты продукта со статичными демо-данными:
  `.tracker-card`+`.roadmap-timeline` (Tracker/Roadmap), `.dna-card` с
  настоящим `renderRadar()` (University DNA), `.lp-swipe-card` — тот же
  класс, что в hero (Match Score vs Admission Reality), `.match-list-row`
  (список Matches). Раз это реальные классы продукта — мокапы автоматически
  совпадают с тем, как эти экраны выглядят в самом приложении, включая
  тёмную тему. Собираются в `landingFeatureCards()` в `app.js`.

  На мобильных (≤760px) scroll-jacking намеренно отключён
  (`initLpFeatureStack()` ничего не биндит, если `window.innerWidth<=760`,
  плюс есть отдельный resize-слушатель на пересечение этой границы) — секция
  превращается в обычный статичный вертикальный список: приём с
  `position:sticky`+abs-позиционированием карточек плохо ведёт себя при
  `height:auto`-контенте на маленьких экранах.

  Переводы `how_title`/`how1..4_title/body` переписаны под новый контент
  (RU и EN); старые CSS-классы четырёх шагов (`.lp-how-stack`,
  `.lp-mini-*` и т.д.) удалены как мёртвый код.

## Фикс: чёрные карточки фич + вылезающий DNA-мокап

Два бага в новой секции "4 фичи" (см. выше), оба в `.lp-feature-card`/
`.lp-feature-mock` в styles.css:

- **Карточки были всегда чёрными** — панель `.lp-feature-card` держала
  фиксированную тёмную палитру (`--lpf-bg:#1E1D18` и т.п.), скопированную
  из присланного макета-референса, и не реагировала на тему сайта. Убрал
  `--lpf-*` совсем, панель теперь на обычных `--brand-surface`/
  `--brand-border`/`--brand-ink` — светлая в светлой теме, тёмная в тёмной,
  как весь остальной лендинг.
- **DNA-мокап вылезал за края карточки** — `.lp-feature-card` была
  `height:460px` фиксированной высотой без `overflow:hidden`; у радара
  DNA (`renderRadar()`, 340×340) вместе с легендой и заголовком реальная
  высота контента больше 460px, поэтому карточка физически не помещала
  DNA-мокап, и он вылезал за скруглённые края панели. Поправил три вещи
  вместе:
  1. `height:460px` → `height:auto; min-height:420px;` — карточка
     подстраивается под свой контент вместо обрезания/вылезания.
  2. Радар для лендинга обёрнут в `.lp-radar-shrink` (`max-width:190px`) —
     сам SVG у `renderRadar()` на `width:100%`, поэтому в узком контейнере
     он просто пропорционально мельче, без искажений; на реальном экране
     DNA в приложении (`renderRadar()` без обёртки) размер не менялся.
  3. `grid-template-columns: 1fr 1.05fr` → `minmax(0,1fr) minmax(0,1.05fr)` —
     без `minmax(0, …)` колонка тянется под `min-content` самого широкого
     ребёнка и может раздвинуть сетку шире фиксированной ширины карточки
     (тот же класс бага, что и с вертикальным overflow, только по
     горизонтали).

  Поскольку высота карточек стала `auto` (разная у каждой из четырёх),
  вертикальное центрирование в pinned-стейдже раньше держалось на
  "угадываемой" браузером статической позиции flex-элемента с
  `position:absolute` — ненадёжно при переменной высоте. Добавил
  `top:50%` на `.lp-feature-card` и `-50%` по Y в JS-трансформе
  (`initLpFeatureStack` в app.js) — теперь центрирование не зависит от
  того, какая из четырёх карточек сейчас высотой выше/ниже остальных.
